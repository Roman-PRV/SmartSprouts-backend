; Image uploads (level covers) are capped at 5 MB on the frontend; align PHP so
; files between PHP's 2 MB default and that cap are not silently rejected by
; PHP-FPM before Laravel's validation runs. post_max_size must stay >= upload.
upload_max_filesize = 6M
post_max_size = 8M
