; Image uploads (level covers) are capped at 10 MB on the frontend; align PHP so
; files between PHP's 2 MB default and that cap are not silently rejected by
; PHP-FPM before Laravel's validation runs. post_max_size stays above upload to
; leave room for the other multipart fields.
upload_max_filesize = 10M
post_max_size = 12M
